{
  "name": "diamond-banquet-hall",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "npm run build && npx serve dist"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  }
}
